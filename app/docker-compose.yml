version: "3.7"

services:

  flask:
    build: ./Flask
    container_name: flask
    restart: always
    environment:
      - APP_NAME=MyFlaskApp
    expose:
      - 8020

  nginx:
    build: ./Nginx
    container_name: nginx
    restart: always
    ports:
      - "8030:8030"